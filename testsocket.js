const io = require('socket.io-client'); const socket = io('https://nightfall-survival-production.up.railway.app', {transports:['websocket']}); socket.on('connect', () => { console.log('Connected!'); socket.emit('create-room', {username:'TEST', platform:'pc'}, (res) => { console.log('Create Response:', res); process.exit(0); }); setTimeout(() => { console.log('Timeout waiting for response'); process.exit(1); }, 3000); });